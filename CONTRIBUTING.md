# 貢獻方式

歡迎回報書籍勘誤與範例問題。請在 Issue 中附上範例檔名、完整命令、作業系統、Python／Playwright 版本與錯誤訊息，並移除帳號、Cookie、網址中的私人識別資料。

提交程式前，請執行：

```bash
python -m unittest discover -s tests/python -p 'test_*.py'
```

外部網站選擇器或資料格式變更，請同時更新操作指南與最後驗證日期。
