# 讀者版目錄

`examples/` 有 16 個正式 Python 範例；根目錄同名舊入口不重複計數。

`docs/guides/` 為操作指南，`book/` 為尚在編寫的書籍大綱、模板與進度。`outputs/` 於執行時自動建立；下載包不含作者資料、新書基準、行情資料庫、登入工作階段、JavaScript 原型與技能副本。

根目錄舊入口保留，但新的教學統一使用 `examples/` 路徑。預設輸出固定在專案的 `outputs/`；自訂相對輸出路徑以當前工作目錄為準。

讀者版的 Blogger 與 Play Books 範例改用環境變數，詳見 [帳號設定](guides/reader-accounts.md)。其餘程式由作者工作區同步；產生與更新方式見 [讀者版同步說明](reader-release.md)。
